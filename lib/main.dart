import 'package:flutter/material.dart';

void main() {
  runApp(const BookDetailsPage());
}

class BookDetailsPage extends StatelessWidget {
  const BookDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // your code here
  }
}
